import { RemoveEscapePipe } from './remove-escape.pipe';

describe('RemoveEscapePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveEscapePipe();
    expect(pipe).toBeTruthy();
  });
});
