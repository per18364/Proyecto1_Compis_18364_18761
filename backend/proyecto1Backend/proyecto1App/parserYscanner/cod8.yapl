class Main inherits IO {
    void main(){
        int x;
        out_string("ingrese un numero:");
        x <- in_int();
        if (x < 0) {
            out_int(0);
        } else {
            out_int(1);
        }
    }
}