class Main inherits IO{
    --int eyes;
    void simpleFn(int z) {
        int x;
        int y;
        x <- 5;
        y <- 3;
        x <- x*y*z;
        out_int(x);
    }

    void main() {
        simpleFn(137);
    }
}