class Main inherits IO {
    void simpleFn(int z) {
        int x;
        int y;
        x <- 5;
        y <- 3;
        x <- x*y*z;
        out_int(x);
    }

    void main() {
        int numero;
        numero <- 137;
        simpleFn(numero);
        
    }
}